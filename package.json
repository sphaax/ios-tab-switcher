{
  "name": "ios-tab-switcher",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only test harness for the Tab Switcher extension (not shipped).",
  "scripts": {
    "shots": "node test/screenshot.js"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
